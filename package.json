{
  "name": "yapa",
  "version": "1.0.0",
  "description": "Yapa — Real-time MERN chat app",
  "main": "index.js",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "author": "Aditya Kadam",
  "license": "MIT"
}
